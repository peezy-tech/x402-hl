name: Publish

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read
  id-token: write

concurrency:
  group: npm-publish-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm-publish
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      - name: Install pnpm
        run: npm install -g pnpm@11.1.3

      - name: Verify tag matches package version
        run: |
          version="$(node -p "require('./package.json').version")"
          expected="v${version}"
          if [ "${GITHUB_REF_NAME}" != "${expected}" ]; then
            echo "Tag ${GITHUB_REF_NAME} does not match package version ${expected}" >&2
            exit 1
          fi

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Unit and adversarial tests
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Typecheck
        run: pnpm typecheck

      - name: Typecheck express example
        run: pnpm example:express:typecheck

      - name: Run compatibility checks
        run: pnpm compat:all

      - name: Check docs configuration
        run: pnpm docs:check

      - name: Build docs
        run: pnpm docs:build

      - name: Pack and audit exact release archive
        id: pack
        run: |
          version="$(node -p "require('./package.json').version")"
          archive="/tmp/x402-hl-${version}.tgz"
          pnpm pack --pack-destination /tmp
          node scripts/pack-audit.mjs "${archive}"
          echo "archive=${archive}" >> "${GITHUB_OUTPUT}"

      - name: Publish to npm
        run: npm publish "${{ steps.pack.outputs.archive }}" --access public --provenance
